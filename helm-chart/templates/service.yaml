apiVersion: v1
kind: Service

metadata:
  name: inventory-api-service

spec:

  selector:
    app: inventory-api

  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000

  type: {{ .Values.service.type }}
