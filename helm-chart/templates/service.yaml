apiVersion: v1
kind: Service

metadata:
  name: inventory-api-service

spec:

  selector:
    app: inventory-api

  ports:
  - name: http
    protocol: TCP
    port: 8000
 

  type: {{ .Values.service.type }}
